#include <stdio.h>

/**
 * main - main function
 * @argc: argument count
 * @argv: argument array
 * Return: 0 for success number otherwise
 */

int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
